import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'demo__seo__v2',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
